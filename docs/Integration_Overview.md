# Integration Overview

Freedomlink1 Unified binds the protocol repository, governance repository, Rootstone-I hardware identity, and PPTF institutional layer through stable JSON maps and version markers.

The `unified/` directory is the integration surface. Version files record the referenced protocol, governance, unified, and lineage revisions.
