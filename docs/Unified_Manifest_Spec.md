# Unified Manifest Specification

The unified manifest is the root declaration for the Freedomlink1 integration layer. It identifies the protocol and governance repositories and resolves the binding manifests under `unified/`.

The manifest is declarative: it describes repository paths, lineage references, institutional bindings, and deployment targets without containing executable code.
