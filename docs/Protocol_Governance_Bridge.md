# Protocol Governance Bridge

`protocol.map.json` maps protocol contracts, interfaces, deployment, CI, documentation, dashboards, and analytics. `governance.map.json` maps lineage, seals, anchors, ledgers, corpus, sentinel, and continuity resources.

Together they define the path bridge between executable protocol assets and sovereign governance records.
