# Rootstone Integration

`rootstone.bindings.json` identifies Rootstone-I as the hardware root and binds its governance identity anchor to the protocol contract surface.

Hardware identity remains referenced by path; private keys and device secrets are intentionally excluded from this repository.
